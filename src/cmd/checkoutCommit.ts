import { GroveContext } from "src/types";
import * as vscode from "vscode";

export function initCheckoutCommit(context: GroveContext) {
    return async (commitHash: string) => {
        // TODO: Checkout commit hash
    };
}
